import React from 'react';
import PageNotFound from '../PageNotFound';

const Dashboard = () => {
	return (
		<div>
			<PageNotFound hasBack={false} full={false} />
		</div>
	);
};

export default Dashboard;
